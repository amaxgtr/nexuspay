spring:
  application:
    name: payment-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/nexuspay_payments
    username: ${DB_USER:nexuspay}
    password: ${DB_PASSWORD:nexuspay}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          batch_size: 50
  kafka:
    bootstrap-servers: ${KAFKA_BROKERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      retries: 3
      properties:
        enable.idempotence: true
        max.in.flight.requests.per.connection: 5

kafka:
  topics:
    payments: nexuspay.payments

gateway:
  timeout-ms: 5000
  max-retries: 3

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    export:
      prometheus:
        enabled: true

logging:
  level:
    io.nexuspay: INFO
    org.springframework.kafka: WARN

# Greenfield: fix(gateway): JWT expiry not validated on token refresh
# 2026-03-28

# Greenfield: feat(gateway): JWT authentication filter with RS256 validation
# 2026-03-28
