name: CI

on:
  push:
    branches: [main, develop, "feature/**", "hotfix/**"]
  pull_request:
    branches: [main, develop]

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: nexuspay
          POSTGRES_PASSWORD: nexuspay
          POSTGRES_DB: nexuspay_test
        ports:
          - 5432:5432

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          cache: maven

      - name: Build and test
        run: mvn clean verify -T 4

      - name: Publish test report
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: JUnit Tests
          path: "**/target/surefire-reports/*.xml"
          reporter: java-junit

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

# Greenfield: feat(shared): CurrencyUtil with ISO 4217 currency handling — NEXUS-2001
# 2026-03-28

# Greenfield: feat(payment): PaymentProcessor with Stripe gateway integration — NEXUS-2014
# 2026-03-28

# Greenfield: feat(payment): Payment JPA entity with audit fields
# 2026-03-28

# Greenfield: docs: README with architecture overview and setup guide
# 2026-03-28

# Greenfield: feat(gateway): Spring Cloud Gateway route configuration
# 2026-03-28

# Greenfield: chore: GitHub Actions CI pipeline with test and build
# 2026-03-28

# Greenfield: feat(shared): CurrencyUtil with ISO 4217 currency handling — NEXUS-2059
# 2026-03-28

# Greenfield: feat(shared): JwtUtil for token generation and validation
# 2026-03-28

# Greenfield: feat(payment): initial PaymentController with charge and capture endpoints — NEXUS-2066
# 2026-03-28

# Greenfield: test(payment): PaymentService unit test suite with Mockito
# 2026-03-28

# Greenfield: feat(payment): initial PaymentController with charge and capture endpoints
# 2026-03-28

# Greenfield: feat(payment): PaymentRequest and PaymentResponse DTOs with validation
# 2026-03-28

# Greenfield: feat(payment): Payment JPA entity with audit fields
# 2026-03-28

# Greenfield: feat(shared): IdempotencyUtil for duplicate request detection
# 2026-03-28

# Greenfield: fix(gateway): JWT expiry not validated on token refresh — NEXUS-2100
# 2026-03-28

# Greenfield: docs: README with architecture overview and setup guide — NEXUS-2102
# 2026-03-28

# Greenfield: feat(shared): PaymentEvent base class with correlation ID
# 2026-03-28

# Decay: fix(compliance): audit entries skipped for batch operations — known issue
# 2026-03-28

# Decay: fix(payment): N+1 query introduced in payment history endpoint — NEXUS-2828
# 2026-03-28

# Decay: fix(payment): race condition workaround with Thread.sleep — NEXUS-2312 — NEXUS-2838
# 2026-03-28

# Decay: fix(settlement): hardcoded FX rate for GBP until API is fixed — NEXUS-2318 — NEXUS-2839
# 2026-03-28

# Decay: fix(ledger): reconciliation skipping zero-amount entries — quick fix
# 2026-03-28

# Decay: fix(payment): another timeout edge case — NEXUS-2301
# 2026-03-28

# Decay: fix(payment): quick patch for merchant X escalation — revert later
# 2026-03-28
